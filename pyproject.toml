[project]
name = "diabayes"
version = "0.1.0"
requires-python = "> 3.0"
authors = [
    { name = "Martijn van den Ende", email = "martijnende@gmail.com" },
]
dependencies = [
    "jax<=0.7.0",
    "jax_tqdm",
    "jaxtyping",
    "equinox",
    "diffrax",
    "optax",
    "optimistix",
    "matplotlib",
    "ipympl",
    "ipython",
    "jupyter",
    "pytest",
]

[project.optional-dependencies]
dev = ["black", "isort", "twine"]
gpu = ["jax[cuda12]<=0.7.0"]
gui = [
    "click",
    "flask",
    "Flask-Migrate",
    "flask-sqlalchemy",
    "flask-socketio",
    "python-dotenv",
    "bokeh",
]
docs = [
    "make",
    "myst-nb",
    "pydata-sphinx-theme",
    "sphinx",
    "sphinx-copybutton",
    "sphinx-togglebutton",
    "sphinx-design",
    "sphinxcontrib-bibtex",
]

[project.scripts]
diabayes = "diabayes.gui.cli:cli"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["diabayes", "diabayes.gui"]

[tool.isort]
profile = "black"

[tool.pytest.ini_options]
minversion = "6.0"
addopts = "-ra -q"
testpaths = ["test"]
pythonpath = ["scripts"]
